{% extends 'base.html.twig' %}

{% block title %}PodCastTime{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 1400px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container mt-5">
  <h1 class="">Hola {{username}} 👋🏻, aquí están tus podcast! 🎤</h1>

  <div class="row">
      {% for podcast in podcasts %}
    <div class="col-md-3">
      <div class="card">
        <img
          src="{{podcast.imagen}}"
          class="card-img-top"
          alt="Fissure in Sandstone"
        />
        <div class="card-body">
          <h5 class="card-title">{{podcast.titulo}}</h5>
          <p class="card-text">
           {{ podcast.descripcion|length > 50 ? podcast.descripcion|slice(0, 50) ~ '...' : podcast.descripcion  }}
          </p>
          <a href="{{
            url('podcast_show', {id: podcast.id})
          }}" class="btn btn-primary">Ir al podcast</a>
        </div>
      </div>

    </div>
      {% endfor %}
  </div>
</div>


  
</div>
</div>
{% endblock %}
